{
  "name": "terahertz-lambda",
  "version": "1.0.0",
  "description": "Lambda function to process WASM sigma/rust validation of an authentication via Nautilus",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "buffer": "^6.0.3",
    "ergo-lib-wasm-nodejs": "^0.19.0",
    "globalthis": "^1.0.3"
  }
}
